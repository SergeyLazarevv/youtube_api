import React from 'react'
import './ShowMoreBtn.css'

let ShowMoreBtn = (props) => {
    return <button className='showMoreBtn' onClick={props.ShowMore}>SHOW MORE</button>
}

export default ShowMoreBtn